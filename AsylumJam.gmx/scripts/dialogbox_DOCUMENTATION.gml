/**

Usage:

var dialogbox = instance_create(topleft_x, topleft_y, obj_dialogbox);
with (dialogbox) {
    dialogbox_set_text("Whatever you want the text to be");
    
    // add all the buttons
    dialogbox_add_button("Button name", script_to_call);
    dialogbox_add_button("Button name", script_to_call);
    ...
}

*/
