/**

This is only for show! You can't actually edit any files, nor is there any button pressing

To update the contents of the file manager (the recycle bin), call this:

with (obj_recyclebinfilesmanager) {
    filemanager_update(array_of_filenames_as_strings, number_of_files)
}

*/

